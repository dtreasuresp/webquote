Perfecto. Ya no quiero eliminar los default, porque los usaremos para que los usuarios que inician sesión por primera vez tengan la oportunidad de visualizar cuál es el contenido que debe ir en cada componente del sistema. Ahora te pido hacer lo sisguiente:
1. Crear una nueva sección en Configuración General para configurar tasas de cambio monetario, o sea, que exista la posibilidad de que los usuarios puedan definir las tasas de cambio monetario en dos variantes: "Manual" (los usuarios declaran las monedas que quieren usar en el sistema y asignan las tasas de cambio que deseen) o "automática" (si está marcada, los usuarios aparece una lista desplegable de proveedores de tasas de cambio en internet, por ejemplo: https://www.bc.gob.cu/tasas-de-cambio y https://eltoque.com/tasas-de-cambio-de-moneda-en-cuba-hoy)
2. Esta nueva sección debe manejar nombre de la moneda, tasa de cambio, segmento de la economía que aplica en el siguiente orden en formato cascada:
2.1. El usuario visualiza dos toggle con diferentes tipos de tasas de cambio (1. General; 2. Fijo 1x24; 3. Fijo 1x120; 4. Flotante)
2.2. Si el usuario selecciona General, aparecen otros dos toggle para configurar las tasas de cambio de dos formas (1. Manual; 2. Automáticas)
2.2.1. Si el usuario selecciona Manual, entonces aparece un modal usando DialogoGenericoDinamico con un formulario coherente visualmente con el el resto de los modales que hemos creado, para que el usuario pueda seleccionar de una lista desplegable la moneda que quiere configurar y otro campo con la tasa de cambio que introduce el usuario.
2.2.2. Si el usuario selecciona automática, entonces aparece un modal usando DialogoGenericoDinamico con un formulario coherente visualmente con el resto de los modales que hemos creado, para que el usuario pueda seleccionar de una lista desplegable los proveedores de información que usará el sistema para importar la información de cambio monetario, en este caso tendremos configurado el sitio del BCC y el sitio ElToque.
2.3. Si el usuario selecciona los toggle 2. Fijo 1x24; 3. Fijo 1x120; 4. Flotante, entonces se va alimentar de la información procedente del BCC de la página https://www.bc.gob.cu/tasas-de-cambio.
3. Integrar todo el sistema monetario del proyecto con la configuración que se declare en esta nueva sección, por ejemplo, si el usuario en el componente Información de CotizaciónTAB define enel campo Moneda que usará USD, entonces se procede a buscar las tasas de cambio que se configuraron en PreferenciasTAb y toda la gestión financiera de la cotización que se está configurando se muestra en la moneda seleccionada con las tasas de cambio activas en ConfiguraciónTAB. 
Dime tu propuesta